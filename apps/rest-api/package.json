{
  "name": "rest-api",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run db:generate && bun run --watch index.ts",
    "db:generate": "bunx drizzle-kit generate"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.22.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@leserver": "workspace:../../packages/@leserver",
    "chalk": "^5.3.0"
  }
}
